apiVersion: core.choreo.dev/v1beta1
kind: ComponentConfig
spec:
  # +optional Incoming connection details for the component (AKA endpoints).
  inbound:
    # +required Unique name for the endpoint. (This name will be used when generating the managed API)
    - name: appointments
      # +required Numeric port value that gets exposed via the endpoint
      port: 8080
      # +required Type of traffic that the endpoint is accepting. For example: REST, GraphQL, etc.
      # Allowed values: REST, GraphQL, GRPC, TCP, UDP.
      type: REST
      # +optional Network level visibility of the endpoint. Defaults to Public
      # Accepted values: Project|Organization|Public.
      networkVisibility: Public
      # +optional Context (base path) of the API that gets exposed via the endpoint.
      # This is mandatory if the endpoint type is set to REST or GraphQL.
      context: /
      # +optional The path to the schema definition file. Defaults to wildcard route if not specified.
      # This is only applicable to REST endpoint types.
      # The path should be relative to the Docker context.
      schemaFilePath: openapi.yaml
  outbound:
    serviceReferences:
    - name: choreo:///pravee/appointment-management/eckwad/8b1c3/v1/ORGANIZATION
      connectionConfig: 01ef0c4d-b6c2-1a7e-b5ea-042d4734b66a
      env:
      - from: ServiceURL
        to: https://59036cfb-152f-4000-90fa-48f78147ac5e-dev-internal.e1-us-east-azure.internal.choreoapis.dev/appointment-management/appointment-service/appointments-8b1/v1
      - from: ConsumerKey
        to: cMaso9Df1g7VTVCb2sbEeopfVYMa
      - from: ConsumerSecret
        to: BvqHjNNMdyHA71hPDkNdqVxhB7ka
      - from: TokenURL
        to: https://sts.choreo.dev/oauth2/token
